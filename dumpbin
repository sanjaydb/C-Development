c:\> dumpbin /all /rawdata:none b.exe
dumpbin /disasm b.exe
dumpbin /dependents p1.exe
dumpbin /dependents /importes p1.exe
dumpbin /directives a.exe  ->  display .drective section of an image
dumpbin /disasm:nobytes a.exe
dumpbin /exports /p.exe
dumpbin /fpo b.exe -> frame pointer optimization (FPO) record -> corecting stackpointer manually
dumpbin /headers a.exe
dumpbin /IMPORTS:msvcrt.dll
dumpbin /linenumbers a.exe -> shuld be compiled with /Zi || /Zd || Z7 || /debug
dumpbin /disasm /range:4219334,0x004061CD p1.exe -> dipslay code 
dumpbin /rawdata:8 p1.exed  -> 2,4,8 byte value or bytes 
dumpbin /relocations p1.exe
dumpbin /rawdata:bytes /section:.data p1.exe
dumpbin /symbols p1.exe -> COFF image should be compiled with /debug 
dumpbin /tls p1.exe -> IMAGE_TLS_DIRECTORY if threads are implemented.
dumpbin /all p1.exe -> Displays all available information about the executable.
dumpbin /clridescription p1.exe -> Displays CLR metadata for .NET assemblies.
dumpbin /clrheader p1.exe -> Displays the CLR header for .NET assemblies.
dumpbin /loadconfig p1.exe -> Displays the load configuration directory (security and initialization info).
dumpbin /section:.text /rawdata p1.exe -> Displays raw data for the .text section.
dumpbin /summary p1.exe -> Provides a summary of sections and their sizes.
dumpbin /exports a.exe -> Lists all exported functions and symbols.
dumpbin /imports a.exe -> Lists all imported functions and symbols.
dumpbin /source p1.obj -> Displays source line information (requires debugging information).
dumpbin /typeinfo p1.exe -> Displays type information from the executable (e.g., COM objects).
dumpbin /fixups p1.exe -> Shows relocation fix-ups in the image.
dumpbin /xref:symbol_name p1.exe -> Displays cross-references for a specific symbol.
dumpbin /hex /disasm a.exe -> Disassembles and displays raw machine instructions in hexadecimal.
dumpbin /hidden p1.exe -> Displays hidden symbols (requires specific debug information).
dumpbin /coff p1.obj -> Displays COFF (Common Object File Format) information.
dumpbin /mapinfo p1.exe -> Displays mapping information for the file.
dumpbin /linkermember p1.lib -> Lists public symbols in a library file.
dumpbin /dependents p1.dll -> Displays dependencies for a DLL.
dumpbin /output:path\to\output.txt p1.exe -> Redirects output to a file for analysis.
dumpbin /debug a.exe -> Displays debug directory information.
dumpbin /machine a.exe -> Identifies the target machine (e.g., x86, x64, ARM).

dumpbin /all p1.exe -> Displays all available information about the executable.
dumpbin /clridescription p1.exe -> Displays CLR metadata for .NET assemblies.
dumpbin /clrheader p1.exe -> Displays the CLR header for .NET assemblies.
dumpbin /loadconfig p1.exe -> Displays the load configuration directory (security and initialization info).
dumpbin /section:.text /rawdata p1.exe -> Displays raw data for the .text section.
dumpbin /summary p1.exe -> Provides a summary of sections and their sizes.
dumpbin /exports a.exe -> Lists all exported functions and symbols.
dumpbin /imports a.exe -> Lists all imported functions and symbols.
dumpbin /source p1.obj -> Displays source line information (requires debugging information).
dumpbin /typeinfo p1.exe -> Displays type information from the executable (e.g., COM objects).
dumpbin /fixups p1.exe -> Shows relocation fix-ups in the image.
dumpbin /xref:symbol_name p1.exe -> Displays cross-references for a specific symbol.
dumpbin /hex /disasm a.exe -> Disassembles and displays raw machine instructions in hexadecimal.
dumpbin /hidden p1.exe -> Displays hidden symbols (requires specific debug information).
dumpbin /coff p1.obj -> Displays COFF (Common Object File Format) information.
dumpbin /mapinfo p1.exe -> Displays mapping information for the file.
dumpbin /linkermember p1.lib -> Lists public symbols in a library file.
dumpbin /dependents p1.dll -> Displays dependencies for a DLL.
dumpbin /output:path\to\output.txt p1.exe -> Redirects output to a file for analysis.
dumpbin /debug a.exe -> Displays debug directory information.
dumpbin /machine a.exe -> Identifies the target machine (e.g., x86, x64, ARM).


cl
--
/FA Assembly code; .asm
/FAc Machine and assembly code; .cod
/FAs Source and assembly code; .asm
/FAcs Machine, source, and assembly code; .cod


Exe Headers
https://docs.microsoft.com/en-us/windows/win32/debug/pe-format?redirectedfrom=MSDN#file-headers

